import 'package:flutter/material.dart';

class AppColors {
  // Primary background color for the entire webpage
  static const Color primaryBackground = Color(0xFF1B1B1B);

  // Secondary background for cards and sections
  static const Color secondaryBackground = Color(0xFF2A2A2A);

  // Accent color used for buttons, icons, and highlights
  static const Color accentOrange = Color(0xFFF25D07);

  // Light text for main headings and content
  static const Color textLight = Color(0xFFFFFFFF);

  // Gray text for descriptions or secondary information
  static const Color textGray = Color(0xFFA3A3A3);

  // Color for hover effects or active states on interactive elements
  static const Color hoverActiveState = Color(0xFFFF7300);

  // Border color for dividers, borders, and outlines
  static const Color borderLight = Color(0xFF404040);
}
